---
- name: Installing Docker
  apt:
    name: "{{ docker_packages }}"
    state: present

- name: Adding "{{ user_name }}" to docker group
  user:
    name: "{{ user_name }}"
    groups: docker
    append: yes