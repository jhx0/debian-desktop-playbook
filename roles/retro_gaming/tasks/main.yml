---
- name: Adding i386 architecture
  shell: dpkg --add-architecture i386

- name: Updating package cache
  apt:
    update_cache: yes

- name: Installing retro gaming packages
  apt:
    name: "{{ retro_gaming_packages }}"
    state: present
